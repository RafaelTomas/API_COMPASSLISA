// const ReserveController = require('../controllers/ReserveController');

// const validationId = require('../validations/id');

// module.exports = (server, routes, prefix = '/api/v1/rental/:id/reserve') => {
//   routes.post('/', ReserveController.create);
//   routes.get('/', ReserveController.find);
//   routes.get('/:_id', validationId, ReserveController.findById);
//   routes.put('/:_id', validationId, ReserveController.update);
//   routes.delete('/:_id', validationId, ReserveController.delete);
//   server.use(prefix, routes);
};
